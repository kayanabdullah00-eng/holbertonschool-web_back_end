#!/usr/bin/env python3
"""Module that contains a type-annotated floor function."""
import math


def floor(n: float) -> int:
    """Return the floor of a floating-point number as an integer."""
    return math.floor(n)
